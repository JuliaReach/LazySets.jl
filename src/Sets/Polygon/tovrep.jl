"""
    tovrep(P::Polygon)

Build a vertex representation of the given polygon.

### Input

- `P` -- polygon in vertex representation

### Output

The same polygon instance.
"""
function tovrep(P::Polygon)
    return P
end
