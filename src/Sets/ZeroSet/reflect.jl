"""
    reflect(Z::ZeroSet)

Concrete reflection of a zero set `Z`, resulting in the reflected set `-Z`.

### Input

- `Z` -- zero set

### Output

The same zero set.
"""
function reflect(Z::ZeroSet)
    return Z
end
