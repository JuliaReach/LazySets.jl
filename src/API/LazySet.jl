
"""
    LazySet

Abstract type for a set of points.

This type is not exported and is only used to define interface methods without
type piracy.

The `LazySets` library defines its own set interface, which is also called
`LazySet`.
"""
abstract type LazySet end
